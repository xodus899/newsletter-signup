# newsletter-signup

##To run locally:
  -clone repository
  - npm modules will need to be installed
    - npm install express
    - npm install request
    - npm install https
    - npm install body-parser
